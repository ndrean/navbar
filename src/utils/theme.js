import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#00000",
      main: "#86b25f",
      dark: "#212c6f",
    },
    secondary: {
      main: "#ffc400",
      light: "#ffd54f",
    },
  },
});

export default theme;

// #3f51b5;
